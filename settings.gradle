pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "CeibaTest"
include ':app'
include ':core'
include ':core-ui'

//Modulo de capa de negocio
include ':users'
include ':users:users_domain'
include ':users:users_data'
include ':users:users_presentation'
